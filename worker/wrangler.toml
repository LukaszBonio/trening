name = "trening-pro-api"
main = "src/index.js"
compatibility_date = "2024-09-01"

# Secrets (set via `wrangler secret put`):
# ANTHROPIC_API_KEY  — klucz do Claude API
# SUPABASE_JWT_SECRET — JWT secret z Supabase Dashboard > Settings > API
