# Trening Pro — zmienne środowiskowe
# Skopiuj do `.env` i wypełnij; `.env` jest gitignored.

# Supabase — projekt z RLS dla auth + sync workouts/body/settings
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxx

# Cloudflare Worker proxy do Claude API (generowanie planów AI)
VITE_AI_PROXY_URL=https://your-worker.workers.dev
